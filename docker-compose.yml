services:
  one-to-ten:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - game-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

volumes:
  game-data:
