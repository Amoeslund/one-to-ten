services:
  one-to-ten:
    image: ghcr.io/amoeslund/one-to-ten:latest
    ports:
      - "3000:3000"
    volumes:
      - game-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

volumes:
  game-data:
